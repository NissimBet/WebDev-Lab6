import {
  GetAllComments,
  GetCommentsForAuthor,
  CreateComment,
  DeleteComment,
  UpdateComment,
} from './commentController';

export const CommentsControllers = {
  GetAllComments,
  GetCommentsForAuthor,
  CreateComment,
  DeleteComment,
  UpdateComment,
};
